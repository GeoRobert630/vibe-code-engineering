"""OWASP ZAP integration: read-only availability detection (detect.py) and the optional, passive
zap-baseline.py adapter (baseline.py, results.py, importer.py). ZAP is never installed or pulled, and no active or
authenticated ZAP scan is run."""
