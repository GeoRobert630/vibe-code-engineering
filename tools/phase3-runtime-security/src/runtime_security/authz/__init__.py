"""Phase 3C authorization / IDOR / BOLA / tenant-isolation *coverage status* (reporting only).

This package performs no requests, reads no credentials and generates no findings. It
only reports that runtime authorization coverage is NOT VERIFIED in this version.
"""
