# vulnerable-node (TEST FIXTURE)

Deliberately insecure code used by the phase2 scanner tests. Every credential in
this directory is FAKE (contains "FAKE") and must never be replaced with a real one.
Do not deploy or run this code.
